\documentclass[manuscript,screen,nonacm]{acmart}

\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,shapes.geometric}

\settopmatter{printacmref=false,printccs=false,printfolios=true}

\title[ProcRosetta: Training Data Generation]{ProcRosetta: Training Data Generation}
\subtitle{Supplementary Material}

\author{Alessandro Berti}
\orcid{0000-0002-3279-4795}
\affiliation{%
  \institution{Process and Data Science (PADS), RWTH Aachen University}
  \city{Aachen}
  \country{Germany}}
\email{a.berti@pads.rwth-aachen.de}

\author{Wil M.P. van der Aalst}
\orcid{0000-0002-0955-6940}
\affiliation{%
  \institution{Process and Data Science (PADS), RWTH Aachen University}
  \city{Aachen}
  \country{Germany}}
\affiliation{%
  \institution{Celonis}
  \city{Munich}
  \country{Germany}}
\email{wvdaalst@pads.rwth-aachen.de}

\renewcommand{\shortauthors}{Alessandro Berti et al.}

\begin{document}
\maketitle

\section{How the Training Data Was Generated}
\label{app:data-generation}

This supplementary document explains the data-generation pipeline used for the
main paper. The goal of the generator is simple: create many small process
behaviors, write each behavior in several forms, and keep an explicit record of
which forms share the same complete visible-trace language. The model can then
learn from paired examples instead of guessing which event log, process tree, and Petri net
belong together. This is the generator's chosen equivalence semantics; it does
not imply equality under every structural or partial-order semantics.

Figure~\ref{fig:app-generator-overview} summarizes the six stages detailed
below.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  font=\footnotesize,
  stage/.style={draw=black!55, rounded corners=2pt, align=center,
    minimum height=10mm, text width=0.22\linewidth, inner sep=3pt},
  flow/.style={-{Latex[length=2mm]}, thick, draw=black!65}
]
  \node[stage, fill=black!4] (choose) {1. Choose\\family type};
  \node[stage, fill=teal!8, right=7mm of choose] (tree)
    {2. Build canonical\\process tree $T$};
  \node[stage, fill=purple!8, right=7mm of tree] (petri)
    {3. Build Petri-net\\variants $G_0,G_1$};
  \node[stage, fill=blue!6, below=8mm of petri] (check)
    {4. Check visible-trace\\equivalence};
  \node[stage, fill=orange!10, left=7mm of check] (eventlog)
    {5. Create event-log\\view $L$};
  \node[stage, fill=black!4, left=7mm of eventlog] (rows)
    {6. Flatten family\\into training rows};

  \draw[flow] (choose) -- (tree);
  \draw[flow] (tree) -- (petri);
  \draw[flow] (petri) -- (check);
  \draw[flow] (check) -- (eventlog);
  \draw[flow] (eventlog) -- (rows);
\end{tikzpicture}
\caption{The behavior-family data-generation pipeline. Equivalence is checked
before an event-log view is created and the family is flattened into rows.}
\Description{A six-stage serpentine flow from choosing a family type, through
process-tree and Petri-net construction and equivalence checking, to event-log
creation and row flattening.}
\label{fig:app-generator-overview}
\end{figure}

\subsection{What One Training Row Contains}
\label{app:data-row}

Each row in the generated data is a \emph{process sample}. A row contains four
main fields:
\begin{enumerate}
  \item a canonical process tree $T$;
  \item a generated event-log view $L$, stored as a finite list of visible activity
        traces;
  \item a Petri-net graph $G$, stored as typed nodes, labeled transitions,
        directed arcs, and initial/final marking vectors;
  \item an equivalence identifier $e$, shared by all rows in the same
        visible-trace equivalence family.
\end{enumerate}
The process tree is the decoder target. The generated event log is the finite
trace-based view of that family. The Petri-net graph is its graph-based view.
During training, rows with the same equivalence identifier are treated as
positive matches for the contrastive objective.

The persisted JSONL rows also store bookkeeping fields:
\texttt{model\_variant\_id}, \texttt{log\_view\_id},
\texttt{representation\_kind}, \texttt{equivalence\_level}, and
\texttt{metadata}. These fields make it possible to inspect which Petri-net
variant was used, which generator motif produced the family, and which
equivalence checks were passed.

\subsection{Current Split and Generator Settings}
\label{app:data-settings}

The current data directory was generated with the behavior-family generator,
fixed seed 13, and two representation rows per behavior family. The split sizes
are:
\begin{center}
\small
\begin{tabular}{lrr}
\toprule
Split & Rows & Behavior families \\
\midrule
Training & 8{,}192 & 4{,}096 \\
Validation & 1{,}024 & 512 \\
Test & 1{,}024 & 512 \\
\bottomrule
\end{tabular}
\end{center}

The important generator settings are:
\begin{itemize}
  \item maximum ordinary-process-tree recursion depth: 8;
  \item minimum ordinary-process-tree depth/size targets: depth 4 and 20 nodes;
  \item maximum activity labels: 30;
  \item maximum operator arity: 3;
  \item traces per generated event-log view: 128;
  \item activity-label reuse probability: 0.15;
  \item early leaf probability during process-tree recursion: 0.65;
  \item representation rows per behavior family: 2;
  \item event-log views per behavior family: 1;
  \item controlled-motif context suffix: 4 fresh activities;
  \item strict minimum motif coverage: 8 families per motif in training and
        4 per motif in validation and test;
  \item enabled process-tree operators: \texttt{SEQ}, \texttt{XOR},
        \texttt{AND}, and two-child \texttt{LOOP}.
\end{itemize}
The \texttt{LOOP} operator is generated in the ordinary-process-tree family. The
generated and decoded loop form is the two-child form
$\operatorname{LOOP}(T_{\mathit{body}},T_{\mathit{redo}})$; the tokenizer mask
therefore emits \texttt{ARITY\_2} after a \texttt{LOOP} token. Because a loop
can repeat an arbitrary number of times, ordinary loop families use finite
process-tree playout to construct the observed trace pool rather than trying to
enumerate an unbounded language exhaustively.

\subsection{Why the Generator Uses Behavior Families}
\label{app:behavior-families}

The generator does not create isolated triples one by one. Instead, it creates
\emph{behavior families}. A behavior family combines one canonical process tree with
two Petri-net representations certified to have the same complete visible-trace
language (or related directly by isomorphic renaming for ordinary loop
families). Both rows in a family share:
\begin{itemize}
  \item the same canonical process tree;
  \item the same generated trace list;
  \item the same equivalence identifier.
\end{itemize}
They differ in their Petri-net graph. Sometimes the second graph is only a
renamed copy of the first graph. In other cases, the second graph uses a
different internal structure while keeping the same complete visible traces.
For example, the concurrency/interleaving family has the same linearized traces
but deliberately differs in partial-order semantics, recorded as
\texttt{partial\_order\_equivalent=false}. Thus the family identifier denotes
the explicit visible-trace equivalence used for training, not equivalence under
all behavioral notions.

The current data is balanced across four family types. Each split contains the
same number of behavior families from each type:
\begin{center}
\small
\begin{tabular}{lrrr}
\toprule
Family type & Training families & Validation families & Test families \\
\midrule
Ordinary process tree & 1{,}024 & 128 & 128 \\
Duplicate label vs. silent routing & 1{,}024 & 128 & 128 \\
Concurrency vs. explicit interleaving & 1{,}024 & 128 & 128 \\
Non-free-choice M pattern & 1{,}024 & 128 & 128 \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Step 1: Choose the Family Type}
\label{app:choose-family-type}

For each split, the generator first computes how many behavior families are
needed. Because the current configuration stores two rows per behavior family,
8{,}192 training rows become 4{,}096 training families. The same calculation
turns 1{,}024 validation rows and 1{,}024 test rows into 512 families each.

The four family types have equal weight. The generator allocates a deterministic
quota to each type and shuffles the order using a seed derived from the global
seed, the split name, and the phrase \texttt{motif-quota-plan}. This gives a
mixed order of samples on disk while preserving exact class balance.

The current run uses strict class coverage. Before replacing the data
directory, the generator verifies that every requested row count is divisible
by the number of rows per family and that the requested family count can meet
the per-motif minimum. An infeasible request therefore fails before the
existing split files are removed. A separate \texttt{best\_effort} mode is
available for small diagnostic datasets; it records motif and representation-
slot deficits instead of requiring them to be zero.

A complete behavior family is assigned to exactly one split, and its
equivalence identifier includes that split. The generator does not, however,
deduplicate complete visible languages across different families or splits.
Consequently, family members cannot leak across a split boundary, but equal or
near-equal behaviors can still occur independently in training and evaluation.

\subsection{Step 2: Build the Canonical Process Tree}
\label{app:build-tree}

The canonical process tree is the target representation used by the decoder.
There are two cases.

\paragraph{Ordinary-process-tree families.}
For ordinary-process-tree families, the generator samples a random process tree. It
first samples a pool of activity labels, then recursively builds the process tree:
\begin{enumerate}
  \item If the maximum depth has been reached, it creates an activity leaf.
  \item Otherwise, with probability 0.65 it also stops early and creates an
        activity leaf.
  \item If it does not stop, it chooses one operator from \texttt{SEQ},
        \texttt{XOR}, \texttt{AND}, and \texttt{LOOP}.
  \item For \texttt{SEQ}, \texttt{XOR}, and \texttt{AND}, it samples an arity
        between 2 and 3 and recursively generates the children.
  \item For \texttt{LOOP}, it recursively generates exactly two children: the
        body and the redo branch.
\end{enumerate}
Activity labels are introduced as \texttt{a0}, \texttt{a1}, and so on. With
probability 0.15 the generator reuses an earlier label instead of introducing a
new one. After the process tree is built, activity labels are canonicalized to
\texttt{A0}, \texttt{A1}, and so on in first-seen order. Children of
\texttt{XOR} and \texttt{AND} nodes are sorted by a canonical structural key, so
equivalent child permutations do not become different targets.

If a generated ordinary process tree has fewer than two unique activity labels, the
generator tries to make it less trivial by adding another activity in sequence.
The sampler makes up to 100 attempts and keeps the first process tree that contains a
loop, reaches the configured minimum depth, and has at least 20 nodes. These are
targets rather than hard postconditions: if no candidate meets all three, the
fallback is the lexicographically highest-scoring candidate, prioritizing loop
presence first, then depth, then size.

Figure~\ref{fig:app-tree-sampler} separates the recursive construction rule
from the candidate-selection rule.

\begin{figure}[H]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
  font=\footnotesize,
  box/.style={draw=black!55, rounded corners=2pt, align=center,
    minimum height=10mm, text width=26mm, inner sep=3pt},
  decision/.style={diamond, draw=black!60, aspect=2.1, align=center,
    text width=16mm, inner sep=1.5pt},
  flow/.style={-{Latex[length=2mm]}, thick, draw=black!65}
]
  \node[box, fill=black!4] (labels) at (0,0)
    {Sample activity-label pool};
  \node[box, fill=teal!8, text width=40mm] (construct) at (3.4,0)
    {Build recursively\\[-1pt]
     depth limit or early stop $\Rightarrow$ activity;\\
     otherwise choose an operator and children};
  \node[box, fill=teal!8, text width=30mm] (canonicalize) at (7.2,0)
    {Canonicalize labels and commutative children};
  \node[box, fill=blue!6, text width=27mm] (score) at (10.3,0)
    {Score loop presence, depth, and size};
  \node[decision, fill=orange!10] (targets) at (10.3,-2.3)
    {targets met?};
  \node[box, fill=green!9] (accept) at (13.3,-2.3)
    {Accept process tree};
  \node[box, fill=orange!8, text width=31mm] (retry) at (7.0,-2.3)
    {Keep best candidate and retry};
  \node[box, fill=black!4, text width=30mm] (fallback) at (3.4,-2.3)
    {After 100 attempts:\\return best score};

  \draw[flow] (labels) -- (construct);
  \draw[flow] (construct) -- (canonicalize);
  \draw[flow] (canonicalize) -- (score);
  \draw[flow] (score) -- (targets);
  \draw[flow] (targets) -- node[above, font=\scriptsize] {yes} (accept);
  \draw[flow] (targets) -- node[above, font=\scriptsize] {no} (retry);
  \draw[flow] (retry) to[out=90,in=-90] node[below left, font=\scriptsize]
    {next attempt} (construct.south);
  \draw[flow] (retry) -- node[above, font=\scriptsize] {attempt limit}
    (fallback);
\end{tikzpicture}%
}
\caption{Ordinary-process-tree sampling. The loop, depth, and size settings are
selection targets: an unmet target triggers another attempt, while the final
fallback returns the highest-scoring candidate.}
\Description{A two-row flowchart showing recursive process-tree construction,
canonicalization, candidate scoring, target checking, acceptance, retry, and
the attempt-limit fallback.}
\label{fig:app-tree-sampler}
\end{figure}

\paragraph{Controlled behavior families.}
The other three family types use small hand-defined canonical process trees:
\begin{itemize}
  \item duplicate-label vs. silent-routing families use
        $\operatorname{XOR}(\operatorname{SEQ}(A0,A1),
        \operatorname{SEQ}(A0,A2))$;
  \item concurrency vs. explicit-interleaving families use
        $\operatorname{AND}(A0,A1)$;
  \item non-free-choice M-pattern families use
        $\operatorname{XOR}(A0,\operatorname{AND}(A1,A2))$.
\end{itemize}
These process trees are intentionally small because their purpose is to isolate one
specific representation issue at a time. To avoid making every controlled case
too bare, the generator appends up to four fresh activity labels as a shared
sequential suffix. The suffix is added as nested binary sequence nodes, so the
configured maximum arity remains 3. For example, if a motif already uses
\texttt{A0}, \texttt{A1}, and \texttt{A2}, the suffix can add \texttt{A3}
through \texttt{A6}. The same suffix is added to both Petri-net variants in the
family.

\subsection{Step 3: Build Two Petri-Net Variants}
\label{app:build-petri-variants}

Each behavior family produces two Petri-net variants. These variants are the
reason the dataset has two rows per behavior family.

\paragraph{Ordinary-process-tree families.}
The first Petri net is obtained by converting the process tree with PM4Py. The
second Petri net is an isomorphic copy: the places and transitions are renamed,
but the arcs, labels, initial marking, and final marking preserve the same
behavior. This teaches the model not to treat arbitrary node names as semantic
information.

\paragraph{Duplicate-label vs. silent-routing families.}
Both variants accept the same two visible traces, $A0,A1$ and $A0,A2$ before the
shared suffix. One variant has two visible \texttt{A0} transitions, one for each
branch. The other variant has a single visible \texttt{A0} transition followed
by silent routing to the \texttt{A1} or \texttt{A2} branch. The visible behavior
is the same, but the graph explains the choice differently.

\paragraph{Concurrency vs. explicit-interleaving families.}
Both variants accept $A0,A1$ and $A1,A0$ before the shared suffix. The first
variant is the Petri net obtained from the parallel process tree, so the two
activities can run concurrently. The second variant lists the two possible
orders explicitly as two serial paths. The visible traces are the same, but one
graph contains true concurrency and the other encodes only the allowed
interleavings; accordingly, the generator records them as not partial-order
equivalent.

\paragraph{Non-free-choice M-pattern families.}
Both variants accept the same visible traces for
$\operatorname{XOR}(A0,\operatorname{AND}(A1,A2))$ before the shared suffix. The
first variant is the standard PM4Py conversion of the block-structured process tree. The
second variant is a hand-built Petri net with a non-free-choice M pattern. This
gives the model examples where behavior is preserved even though the Petri-net
structure is less block-like.

\subsection{Step 4: Check Behavioral Equivalence}
\label{app:equivalence-check}

For every family not handled by the ordinary-loop isomorphism shortcut, the
generator compares the visible trace sets returned for all Petri-net variants.
The checker explores firing sequences and records search states comprising the
marking, visible prefix, and firing count. It records traces that reach the
final marking, omits invisible transitions, and reports whether exploration
finished or a resource bound truncated it.

The current check uses these bounds:
\begin{itemize}
  \item a threshold of 5{,}000 visited search states;
  \item a threshold of 10{,}000 distinct complete visible traces;
  \item at most 32 visible activities per trace;
  \item at most $4\cdot32+|T|$ transition firings along a search path, where
        $|T|$ is the number of Petri-net transitions.
\end{itemize}
The implementation flags the search as incomplete when either count threshold
is exceeded or either path bound prevents further exploration. The trace set
returned up to that point must be nonempty and identical for every variant. If
all searches finish, the certificate status is \texttt{exact}; if a bound
truncates at least one search, the compared finite result receives status
\texttt{bounded}. The current configuration sets
\texttt{exact\_equivalence\_only\_for\_training} to false, so a bounded family
is eligible for any split. In the persisted data used here, however, every
controlled family received an \texttt{exact} certificate; there are no bounded
rows.

Ordinary loop families are handled separately because their language is
unbounded. Their second Petri net is an isomorphic clone of the PM4Py process-tree
conversion, so equivalence is certified structurally by the renaming
transformation. All ordinary-process-tree rows in the persisted data therefore store
the status \texttt{isomorphic}. Each accepted row includes the certificate
status, semantics, checked variant names, and reference-language size in its
metadata.

The generator also checks simple structural health conditions. In particular,
the final marking must be reachable and there must be no dead transitions. A
family that fails these checks is rejected.

\subsection{Step 5: Create the Generated Event-Log View}
\label{app:create-log-view}

After equivalence is checked or certified, the generator creates a trace pool.
For controlled families in the current data, the pool is the sorted exact
visible language returned by the completed checker. For ordinary loop
families, PM4Py performs $\max(256,4n)$ process-tree playouts for requested event-log
size $n$; the generator then deduplicates and sorts the observations. Thus the
current $n=128$ run draws 512 playout traces before deduplication. It creates
one event-log view per family and stores 128 traces in that view.

The current configuration lists \texttt{uniform\_variants} followed by
\texttt{resampled}. Because it requests only one event-log view, that view uses the
first mode. In \texttt{uniform\_variants}, the generator cycles through the
trace pool until it has 128 traces, then shuffles them with a deterministic
seed. If a behavior has only two variants, both recur; if the pool has more
than 128 variants, only its first 128 sorted entries are used before shuffling.

The generator also supports sparse, long-tail, incomplete, resampled, and noisy
modes for other experiments. The persisted split uses one clean
\texttt{uniform\_variants} view per family; all stored trace-edit lists are
empty.

Family construction, quota order, and event-log-view shuffling use stable seeds;
loop trace pools are an exception: although a derived playout seed is recorded,
the current call does not apply it to PM4Py's playout randomness. Exact loop
trace pools are therefore not guaranteed by the generator seed alone and can
depend on ambient random state as well as the installed PM4Py version. The
persisted JSONL rows, rather than regeneration from seed alone, define the exact
loop event-log contents used in the study.

\subsection{Step 6: Flatten Families into Training Rows}
\label{app:flatten-rows}

The final step turns each behavior family into rows. With the current
configuration, each family has one generated event-log view and two Petri-net
variants, so it becomes exactly two rows:
\[
\begin{aligned}
  &(\text{process tree}, \text{same event-log view},
    \text{Petri-net variant 0}, e),\\
  &(\text{process tree}, \text{same event-log view},
    \text{Petri-net variant 1}, e).
\end{aligned}
\]
Figure~\ref{fig:app-flatten-family} makes the shared and variant-specific fields
explicit.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  font=\footnotesize,
  shared/.style={draw=black!55, rounded corners=2pt, align=left,
    text width=0.30\linewidth, minimum height=24mm, inner sep=5pt,
    fill=black!3},
  variant/.style={draw=purple!65!black, rounded corners=2pt, align=center,
    text width=0.18\linewidth, minimum height=10mm, inner sep=3pt,
    fill=purple!8},
  row/.style={draw=teal!60!black, rounded corners=2pt, align=center,
    text width=0.25\linewidth, minimum height=13mm, inner sep=3pt,
    fill=teal!7},
  flow/.style={-{Latex[length=2mm]}, thick, draw=black!65}
]
  \node[shared] (shared) {\textbf{Shared family fields}\\
    canonical process tree $T$\\
    event-log view $L$\\
    equivalence identifier $e$};
  \node[variant, above right=1mm and 13mm of shared] (gzero)
    {Petri-net variant $G_0$};
  \node[variant, below right=1mm and 13mm of shared] (gone)
    {Petri-net variant $G_1$};
  \node[row, right=13mm of gzero] (rowzero)
    {training row 0\\$\bigl(T,L,G_0,e\bigr)$};
  \node[row, right=13mm of gone] (rowone)
    {training row 1\\$\bigl(T,L,G_1,e\bigr)$};

  \draw[flow] (shared.east) -- (gzero.west);
  \draw[flow] (shared.east) -- (gone.west);
  \draw[flow] (gzero) -- (rowzero);
  \draw[flow] (gone) -- (rowone);
\end{tikzpicture}
\caption{Flattening one behavior family into two training rows. The process
tree, event-log view, and equivalence identifier are duplicated; only the
Petri-net graph changes.}
\Description{Shared process-tree, event-log, and equivalence fields branch to
two Petri-net variants, which produce two corresponding training rows.}
\label{fig:app-flatten-family}
\end{figure}

Both rows have the same equivalence identifier $e$. The process tree and trace
list are identical in the two rows. The Petri-net graph is different.

Rows are written to:
\begin{itemize}
  \item \texttt{data/training/samples.jsonl};
  \item \texttt{data/validation/samples.jsonl};
  \item \texttt{data/test/samples.jsonl}.
\end{itemize}
The generator also writes \texttt{data/metadata.json}, which records the seed,
the full synthetic configuration, split sizes, motif counts, representation
counts, average process-tree and trace statistics, and maximum Petri-net graph sizes.
For each split it records both planned and actual motif quotas, per-motif
representation-slot counts, any strict-coverage deficits, and whether the
minimum-coverage check passed. During generation, \texttt{sample.py} reports
completed flattened rows with a per-split progress bar on standard error;
\texttt{--quiet} disables this display without changing the generated data.

\subsection{How the Row Is Used During Training}
\label{app:training-use}

At training time, each row is converted into tensors:
\begin{itemize}
  \item the process tree becomes a prefix token sequence with explicit arity
        tokens;
  \item the generated traces become padded activity-token sequences;
  \item the Petri net becomes node-type indices, transition-label ids, marking
        features, and two adjacency matrices.
\end{itemize}
The batch collator also creates a positive-pair mask from the equivalence
identifiers. If two rows have the same equivalence identifier, they are treated
as matching views of the same visible-trace equivalence family. Training batches
can also apply a consistent random renaming of activity labels within a family.
This preserves the behavior while reducing dependence on the literal label
names.

In short, the data-generation pipeline makes the supervision explicit. The
model sees a process tree, a finite generated event log, and a Petri-net graph; it also sees
which rows belong to the same visible-trace equivalence family. The generated
families therefore train the model to align artifact types by behavior rather
than by node names, transition counts, or one particular Petri-net structure.

\end{document}
