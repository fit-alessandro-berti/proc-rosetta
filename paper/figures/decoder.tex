\begin{figure}[t]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
 state/.style={draw,rounded corners,minimum width=27mm,minimum height=10mm,align=center,font=\small},
 flow/.style={-{Stealth[length=2mm]},thick},
 guard/.style={font=\scriptsize,align=center,text=prgray}
]
\node[state,fill=prlightblue] (open) {open slots $s>0$};
\node[state,fill=prlightteal,right=24mm of open] (emit) {emit legal token\\$s\leftarrow s-1+\mathrm{arity}$};
\node[state,fill=prlightgold,right=24mm of emit] (closed) {$s=0$\\EOS enabled};
\node[state,fill=prlightred,below=16mm of emit] (budget) {budget exhausted\\shortest legal closure};
\node[state,fill=prlightteal,right=24mm of budget] (check) {parse, fold,\\convert and audit};
\draw[flow] (open)--node[guard,above]{grammar + source alphabet\\+ duplicate mask}(emit);
\draw[flow] (emit) to[bend left=18] node[guard,above]{still open} (open);
\draw[flow] (emit)--node[guard,above]{all children filled}(closed);
\draw[flow] (emit)--node[guard,left]{length limit}(budget);
\draw[flow] (budget)--(check);
\draw[flow] (closed)|-(check);
\end{tikzpicture}
}
\caption{Deployment decoding state. Bounded completion guarantees a syntactically closed prefix within the supported grammar; conversion and behavioral evaluation remain distinct postconditions.}
\label{fig:decoder}
\end{figure}
